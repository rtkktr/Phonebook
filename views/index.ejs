<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/welcome.css">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background: url('/img/phonebookimg.jpg') no-repeat center center fixed;
            background-size: cover;
            height: 100vh;
        }

        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(5px);
            z-index: 1;
        }

        .content {
            position: relative;
            z-index: 2;
        }

        .text-white {
            color: #fff;
        }
    </style>
</head>
<body>
    <div class="container-fluid d-flex justify-content-center align-items-center min-vh-100">
        <div class="overlay"></div>
        <div class="content text-center">
            <% if (user) { %>
                <h1 class="display-4 text-white">Welcome <%= user.fullname %>!</h1>
            <% } else { %>
                <h1 class="display-4 text-white">Welcome to the Online Phone Book</h1>
                <div class="mt-4">
                    <a href="users/signin" class="btn btn-primary btn-lg mr-3">Sign In</a>
                    <a href="users/signup" class="btn btn-secondary btn-lg">Sign Up</a>
                </div>
            <% } %>
            
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>